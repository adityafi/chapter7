{
  "name": "chapter7",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sequelize:create_migration": "sequelize-cli migration:generate --name",
    "sequelize:migrate": "sequelize-cli db:migrate",
    "sequelize:migrate:undo": "sequelize-cli db:migrate:undo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.1.4",
    "pg": "^8.11.0",
    "sequelize": "^6.32.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.1"
  }
}
